Program requirements: Java 11 SDK or runtime.


The program can be started by running ./run on linux or run.cmd on windows.

If it complains about the Version number of your SDK being too low (Java Class version 52 equals Java 8) make sure you set the JAVA_PATH variable inside the script to a suitable Java 11 /bin directory.
